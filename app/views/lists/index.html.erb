<div class="container mt-3">
  <h1>My Lists</h1>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list), class: 'text-primary text-decoration-none'%></li>
    <% end %>
    <br>
    <%= link_to "Create a movie list", new_list_path, class:"btn btn-primary"%>
</div>
